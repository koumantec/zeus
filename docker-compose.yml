version: '3.8'
services:
  zeus:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ~/.core:/root/.core
    environment:
      - SERVER_PORT=3000
      - ANSIBLE_HOST=local
      - DEMO_MODE=true
    restart: unless-stopped
