spring:
  datasource:
    url: jdbc:sqlite:target/test-docker-${random.uuid}.db
  flyway:
    enabled: true
    locations: classpath:db/migration

core:
  api:
    token: test-token

orchestrator:
  worker:
    enabled: true
    autostart: false
