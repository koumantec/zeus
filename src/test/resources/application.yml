server:
  port: 8000

spring:
  application:
    name: core-control
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:/tmp/app.db
  flyway:
    enabled: true
    locations: classpath:db/migration
    mixed: true

logging:
  level:
    root: INFO
    com.stetits.core: INFO
