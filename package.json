{
  "name": "core-monitor",
  "version": "1.0.0",
  "description": "A beautiful dashboard to view remote docker container status via Ansible",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}